---
- name: restart v2ray (Archlinux)
  become: true
  systemd:
    name: v2ray
    state: restarted
  when:
    - ansible_facts.distribution == 'Archlinux'
  tags:
    - sudo

- name: restart v2ray (MacOSX)
  command: brew services restart v2ray-core
  when: ansible_facts.distribution == 'MacOSX'
